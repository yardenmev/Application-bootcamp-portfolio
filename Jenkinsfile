pipeline {
    agent any
    stages {
        stage('Build image') {
            steps {
                // sh 'docker build -t yarden-todo:1 .'
                // sh 'docker images'
                sh 'git tag -l'
            }
        }
    }    
            
}